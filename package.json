{
  "name": "simple-js-server",
  "version": "1.0.0",
  "description": "A simple server to serve mulltiple js files as one in a single endpoint",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "tsc"
  },
  "bin": {
    "sjss": "./index.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.8.10"
  },
  "dependencies": {
    "tsc": "^2.0.4"
  }
}
